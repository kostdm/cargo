const getAll = require('./all');
const getById = require('./one');
const addNew = require('./new');
const deleteById = require('./delete');
const editById = require('./edit');

module.exports = {
  getAll,
  getById,
  addNew,
  deleteById,
  editById,
};
